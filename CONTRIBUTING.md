# 기여 가이드

## 브랜치

모든 작업 브랜치는 최신 `develop`에서 생성합니다.

```text
feature/<작업명>   기능 구현
fix/<작업명>       오류 수정
model/<작업명>     모델 학습·평가
data/<작업명>      데이터·피처 파이프라인
chore/<작업명>     환경·도구·저장소 설정
docs/<작업명>      문서 작업
```

## 커밋

커밋 제목은 `type: 작업 요약` 형식을 사용합니다.

```text
feat: 재구매 예측 API 추가
model: 재구매 베이스라인 모델 구현
data: 유효 구매 데이터 생성 로직 추가
test: 구매 취소 처리 테스트 추가
chore: Python 개발환경 구성
```

사용 가능한 type은 `feat`, `fix`, `model`, `data`, `refactor`, `test`, `docs`, `chore`입니다.

## Pull Request

- 일반 작업은 `develop`을 대상으로 생성합니다.
- `main`에는 직접 Push하지 않습니다.
- PR 템플릿의 검증 결과와 영향 범위를 작성합니다.
- 한 PR에는 하나의 목적만 포함합니다.
- 데이터 원본, 모델 파일, API 키와 비밀번호를 포함하지 않습니다.

## 완료 기준

- 담당 모듈 경계를 지켰습니다.
- 재현 가능한 실행 방법을 문서화했습니다.
- 새 로직에 필요한 테스트를 추가했습니다.
- 로컬 테스트와 코드 품질 검사를 통과했습니다.
